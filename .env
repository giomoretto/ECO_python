# Root of the acados installation (the directory containing lib/ and include/).
# This is the only path ECO needs; everything else is derived from it.
ACADOS_SOURCE_DIR=/home/morettog/projects/phd/acados
